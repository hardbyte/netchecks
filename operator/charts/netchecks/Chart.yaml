apiVersion: v2
name: netchecks
description: Netchecks proactively verifies whether your security controls are working as intended.
type: application
icon: https://raw.githubusercontent.com/hardbyte/netchecks/main/.github/logo.png
# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.5.0

# This is the version number of the application being deployed. This version number should be
# incremented each time you make changes to the application. Versions are not expected to
# follow Semantic Versioning. They should reflect the version the application is using.
# It is recommended to use it with quotes.
appVersion: "0.11.0"
sources:
  - https://github.com/hardbyte/netchecks
# The maintainer name must be a GitHub username (validated by ct lint).
maintainers:
  - name: hardbyte
    email: brian@hardbyte.nz
    url: hardbyte.nz
annotations:
  # https://artifacthub.io/docs/topics/annotations/helm/
  artifacthub.io/changes: |
    - kind: added
      description: "CRD installation is configurable: crds.install, crds.keep and per-API-group toggles (crds.groups.netchecks, crds.groups.wgpolicyk8s). The CRDs moved from crds/ into templates/; see the README for the one-time adoption step when upgrading from chart 0.3.x."
    - kind: fixed
      description: "The operator discovers the served wgpolicyk8s.io PolicyReport API version at runtime (v1beta1 or v1alpha2) instead of requiring v1beta1, so crds.groups.wgpolicyk8s=false works with Kyverno-owned CRDs."
    - kind: changed
      description: "Operator 0.11.0: kube-rs 4 / k8s-openapi 0.28; a missing PolicyReport CRD is reported as a PolicyReportApiUnavailable status condition."
  artifacthub.io/category: security
  artifacthub.io/operator: "true"
  artifacthub.io/prerelease: "false"
  artifacthub.io/operatorCapabilities: Deep Insights
  artifacthub.io/license: Apache-2.0
  artifacthub.io/links: |
    - name: Documentation
      url: https://netchecks.io
  artifacthub.io/crds: |
    - kind: NetworkAssertion
      version: v1
      name: networkassertions.netchecks.io
      displayName: Network Assertion
      description: NetworkAssertion is the Schema for the networkassertions API
  artifacthub.io/crdsExamples: |
    - apiVersion: netchecks.io/v1
      kind: NetworkAssertion
      metadata:
        name: http-should-work
        annotations:
          description: Assert pod can connect to k8s API
      spec:
        template:
          metadata:
            labels:
              optional-label: applied-to-test-pod
            annotations:
              my: override-annotation
        rules:
          - name: kubernetes-version
            type: http
            url: https://kubernetes.default.svc/version
            verify-tls-cert: false
            expected: pass
            validate:
              message: Http request to Kubernetes API should succeed.
dependencies:
  - name: policy-reporter
    version: 3.7.4
    repository: https://kyverno.github.io/policy-reporter
    condition: policy-reporter.enabled
